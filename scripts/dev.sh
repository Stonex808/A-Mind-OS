#!/usr/bin/env bash
set -euo pipefail

echo "[Refocus-OS] dev runner"
echo "Starting: kernel_hook → LG-C → LG-A → Orchestrator → Verifier → HUD"

# These are placeholders for your local commands or uvicorn/npm scripts.
echo "(placeholder) launch kernel_hook"
echo "(placeholder) launch lg_c_syssec"
echo "(placeholder) launch lg_a_langsec"
echo "(placeholder) launch orchestrator"
echo "(placeholder) launch verifier_trm"
echo "(placeholder) launch HUD"

echo "Tip: Select text anywhere and press Alt+Enter to send an intent."
